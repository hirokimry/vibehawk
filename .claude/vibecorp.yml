# vibecorp.yml — プロジェクト設定
name: vibehawk
preset: full
language: ja
base_branch: main
protected_files:
  - MVV.md
coderabbit:
  # Issue #327: CodeRabbit を停止し vibehawk の status check 単独を merge gate にするため無効化
  enabled: false
claude_action:
  # Issue #327 以降、vibehawk レビュー単独運用のため無効化（autonomous-restrictions.md §6 参照）
  enabled: false
  skip_paths:
    - "*.lock"
    - ".git/**"
    - "node_modules/**"
    - "dist/**"
    - "build/**"
    - ".cache/**"
    - "vendor/**"
branch_protection:
  # Issue #327: merge gate は vibehawk の status check 単独。AI approve が人間レビュー必須要件を
  # バイパスする構造を避けるため、required_approving_review_count は 0 とする（README §設計思想）。
  required_approvals: 0
diagnose:
  enabled: true
  # 1 run あたりの起票上限。課金超過防止のため意図的に抑制（docs/cost-analysis.md 参照）
  max_issues_per_run: 7
  # 1 日あたりの起票上限（= max_issues_per_run × 2）
  max_issues_per_day: 14
  max_files_per_issue: 10
  scope: ""
  forbidden_targets:
    - "skills/**"
    - "hooks/*.sh"
    - "vibecorp.yml"
    - "MVV.md"
    - "SECURITY.md"
    - "POLICY.md"
plan:
  review_agents:
    - architect
    - security
    - testing
    - performance
    - dx
    - cost
    - legal
