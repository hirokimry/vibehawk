# SM 判断記録 2026-Q2

## 2026-05-18 — Issue #167 / #170 / #171 並列実行可否分析

### 分析対象

- Issue #167: vibehawk-review の auto_resolve が Claude → workflow step に移管（intent/refactor）
- Issue #170: vibehawk-review の review body 冒頭「使用設定: $CONFIG_SOURCE」削除（intent/bugfix）
- Issue #171: vibehawk-review が新規 inline 1 件以上で REQUEST_CHANGES、全 resolve で APPROVE に切り替わる（intent/feature）

### 実ファイル確認結果

`templates/.github/workflows/vibehawk-review.yml`（713行）の変更セクションを実読して特定:

| Issue | 変更セクション | 行範囲 |
|-------|-------------|--------|
| #167 | Claude prompt 内「## auto_resolve」セクション（resolveReviewThread 削除） + claude_args の --json-schema（resolved_thread_ids 追加）+ 新規 auto_resolve step（decide_event の前に追加） | 375–392, 471, 539前後 |
| #170 | Claude prompt 内「### 設定ソース表示」1 段落（行443） | 443 |
| #171 | decide_event step の判定ルール run: ブロック（行520–533） + docs/specification.md | 520–533 |

### 結論

**#170 と #171 は並列可能。#167 は #170/#171 と直列が必要。**

根拠:

1. **#170 vs #171**: 編集セクションが完全に分離している。#170 は prompt 内 443 行目付近の 1 段落削除、#171 は decide_event step の run: ブロック（520–533）と specification.md の変更。同一ファイルだが別セクション。テスト側も #170 は `test_workflow_template_snapshot.sh` + `test_workflow_yaml.sh` の CONFIG_SOURCE 関連 assertion（526–533 行）、#171 は `test_bundled_review_post_step.sh` + `test_workflow_yaml.sh` の decide_event 関連 assertion（860–1015 行）で分離している。行レベルで重複なし → **並列可能**。

2. **#167 vs #171**: #167 が decide_event step の**前**に新規 auto_resolve step を追加し、かつ decide_event step が auto_resolve 後の unresolved 状態を参照している（行488「Claude が直前に resolveReviewThread mutation を呼んでいるため」という前提が変わる）。#171 は decide_event の判定ロジックそのものを変更する。auto_resolve を workflow step に移管するとその副産物（空の COMMENTED review）が変わり、#171 の「全 resolve で APPROVE」ロジックのテスト前提も変わる可能性がある。同じ step 群（auto_resolve → decide_event）を同時に変更すると意味論的な衝突リスクがある → **直列が安全（#167 先行）**。

3. **#167 vs #170**: #167 は行 375–392（auto_resolve セクション）と行 471（claude_args schema）と新規 step。#170 は行 443（設定ソース表示段落）。行レベルでは重複しないが、#167 が claude_args の --json-schema に `resolved_thread_ids` を追加するため、schema 文字列が変わる。#170 はその行に触れない。しかし #167 が新規 step を挿入することで test_workflow_yaml.sh の step 順序検証（行883–897）が影響を受ける可能性がある。test_workflow_yaml.sh を両 Issue が同時に変更した場合、assertion の追加箇所が近接する可能性もある。安全側に倒す原則から **直列を推奨（#167 先行）**。

### テストファイルの競合評価

`test_workflow_yaml.sh`: #167/#170/#171 全てが追加/変更する可能性があるが、それぞれの assertion 追加箇所はファイル末尾（既存テストは行 1017 で終了）または既存 assertion の修正であり、コンフリクトは assertion の近接による可能性に限られる。#170 と #171 の assertion は既存ファイルの別セクションを参照しており、行レベルの重複は低い。

`test_workflow_template_snapshot.sh`: #170 のみが新規 assertion を追加する可能性がある（CONFIG_SOURCE 冒頭表示の不在を検証）。#167/#171 は触れない想定。

`test_bundled_review_post_step.sh`: #171 のみが判定ロジック変更に伴う新規テストケースを追加する可能性がある。#167/#170 は触れない想定。

### 並列可能グループ

- グループ A（並列可）: #170 + #171
- グループ B（直列先行）: #167

### 直列チェーン

1. #167（auto_resolve workflow step 移管）
2. #170 / #171（並列）

