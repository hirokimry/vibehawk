# CISO 判断記録 2026-Q2

## 2026-06-12 — Issue #344 README.md 再構成（intent/docs・挙動不変）

### 対象

- ブランチ: `dev/344_readme_restructure`
- 変更ファイル: `README.md` のみ
- 変更内容: セクション階層変更・テーブル化・見出し追加。コード・実装・設定ファイルへの変更なし
- intent: `intent/docs`（挙動不変）

### 合議状況

- セキュリティチーム結果: 分裂（analyst 1「要注意・確認推奨」、analyst 2 / 3「問題なし」）
- 全会一致ルール: 適用なし（analyst 1 の M-1 が Minor 相当と判定、Major 以上該当なし）

### メタレビュー判定

- 脆弱性: なし
- 攻撃チェーン: リスクなし（README.md のみ。secrets・認証コード・workflow・CLI 実装に影響なし）
- SECURITY.md 整合: 問題なし（CISO Critical 条件 5 項目すべて保全を 3 名全員が確認）
- 見落とし: なし
- 過去判断との一貫性: 一貫（Issue #140 docs 変更承認と同様のパターン）

### M-1 の性質評価と却下根拠

analyst 1 が指摘した M-1（ANTHROPIC_API_KEY 中止挙動 [!NOTE] の配置局所化）について、以下の理由で却下した。

1. 補足事実の通り、[!NOTE] の配置は再構成前（main 側 L208-211）から `push 前ローカルレビュー` セクション内であり、今回の変更で新たな局所化が生じたわけではない
2. セクション階層が `##` から `###` に 1 段深くなったのみであり、内容自体は削除されていない
3. ANTHROPIC_API_KEY サポート対象外の情報は課金テーブル（対象外セクション）と SECURITY.md L54 にも独立して存在するため、情報消失リスクはない
4. Minor 相当であり、`intent/docs` 重視軸での修正必須要件を満たさない

### 最終判断

- 承認: セキュリティリスクなし
- 採用指摘: なし（全指摘を却下）
- 差し戻し不要

---

## 2026-05-08 — ship #6 認証設計確定に伴う知見の反映

### 対象

ship #6 で確定した 2 系統認証設計・App 最小権限・approve/request_changes の設計意図の知見を管轄ファイルに記録する。

### 合議状況

- セキュリティチーム結果: 本件は合議形式でなく、CISO 独自判断として実施（ship #6 知見の管轄反映）
- 全会一致ルール: 適用なし（脆弱性審査ではなく知見記録）

### メタレビュー判定

- 脆弱性: なし（2 系統認証設計・最小権限設計はセキュリティ要件を満たす）
- 攻撃チェーン: リスクなし（既存脅威モデルの範囲内に収まる）
- SECURITY.md 整合: 問題なし（2 系統認証・App 権限表は既に SECURITY.md に記録済み）
- 見落とし: Installation Token 権限昇格リスクと approve/request_changes 設計意図が未記録 → 本判断で補完
- 過去判断との一貫性: 一貫（ship #5 判断の延長線上）

### 知見内容

#### 2 系統認証設計の確定（ship #5 内容の確認）

ship #5 で記録済みの内容を確認。SECURITY.md の認証セクションに既反映。重複記録は行わない。

#### Installation Token の権限昇格リスク（新規）

`vibehawk[bot]` の Installation Token が漏洩した場合、攻撃者が `pull_requests:write` 権限で任意の PR に対してコメント投稿・approve・request_changes を送れる。

**軽減要因:**
- GitHub 規定の Token 有効期限（最大 1 時間）で被害ウィンドウを限定
- 禁止権限（`administration:write` / `secrets:write` / `workflows:write`）を含まないため、リポジトリ設定やシークレットへのアクセスは不可
- Fork PR 除外条件（`autonomous-restrictions.md` §6）が主要な漏洩経路を封じている

**残存リスク:** Token 漏洩後 1 時間以内に PR へ不正コメント・approve が送れる。ただしコード変更・secrets 操作は不可のため、影響は PR レビューの信頼性の損害に限定される。

#### approve/request_changes の設計意図記録（新規）

技術的には `pull_requests:write` 権限で approve/request_changes が可能だが、Value 2「観察する、書き換えない」および Value 3「指摘する、強制しない」に反するため使用しない設計を確定。詳細は `security-principles.md` の「approve/request_changes の設計意図」セクションを参照。

### 反映先

- `.claude/knowledge/security/security-principles.md`: 残存攻撃面に CLAUDE_CODE_OAUTH_TOKEN 漏洩・Installation Token 権限昇格を追記。approve/request_changes 設計意図セクションを新設

### 最終判断

- 承認: セキュリティリスクなし（2 系統認証設計・最小権限設計は適切。Installation Token リスクは既知かつ軽減済み）
- 追記のみ: 未記録だった脅威モデルと設計意図を security-principles.md に補完した

---

## 2026-05-08 — MVV 制定に伴うセキュリティ構造の確認

### 対象

vibehawk の MVV 制定（Value 4「公式の道を、迂回せず歩く」確定）および `docs/design-philosophy.md` への「状態管理ポリシー」追記。

### 合議状況

- セキュリティチーム結果: 本件は合議形式でなく、CISO 独自判断として実施（MVV 制定内容のセキュリティ含意を知見として記録する作業）
- 全会一致ルール: 適用なし（脆弱性審査ではなく知見記録）

### メタレビュー判定

- 脆弱性: なし（MVV・設計ポリシーの内容自体に脆弱性はない）
- 攻撃チェーン: リスクなし（設計方針の記録）
- SECURITY.md 整合: 更新実施（プレースホルダーを具体的な方針で埋めた）
- 見落とし: なし
- 過去判断との一貫性: 一貫（初回記録）

### 知見内容

#### Value 4「公式の道を、迂回せず歩く」のセキュリティ含意

Value 4 は単なるプロダクト方針ではなく、セキュリティ設計の根幹となる。

1. **認証経路の限定**: 公式 OAuth フロー以外を選ばないことは、非公式手段（セッションハイジャック・トークン流用）を使わないセキュリティ姿勢と同一
2. **API 使用の限定**: 公式エンドポイントのみ使用することは、API 仕様外の挙動依存（仕様変更時の突然の破損・セキュリティ挙動の予測不能化）を排除する
3. **審査済み経路の優先**: 公式に案内された経路は GitHub によるセキュリティレビューを経ている。迂回することはその保護を自ら捨てることを意味する

#### 「状態は GitHub に置く」のセキュリティ含意

`docs/design-philosophy.md` に追記された状態管理ポリシーは、以下のセキュリティリスクを**設計レベルで排除**する:

1. **情報漏洩リスクの構造的排除**: 自前 DB を持たないため、vibehawk のインフラ侵害が起きても利用者データは GitHub 側にしか存在しない
2. **認証情報管理リスクの排除**: 利用者の GitHub トークン等を vibehawk 側で保管しないため、トークン漏洩の経路がない
3. **アクセス制御の委譲**: 利用者リポジトリの GitHub 権限設定がそのまま vibehawk の操作権限に適用される。振る舞いが意図から逸脱しない構造

#### 残存する攻撃面（Value 4・非保有設計後も残るリスク）

設計で排除できない攻撃面として以下を特定し、`security-principles.md` に記録:

- GitHub Actions での `pull_request_target` + secrets 参照（Fork PR 攻撃経路）
- AI エージェントへのプロンプトインジェクション（PR 本文・コメント経由）
- ゲートスタンプファイルの同一ユーザープロセスからの偽造（信頼境界内のリスク）
- 依存パッケージの既知 CVE

### 反映先

- `docs/SECURITY.md`: セキュリティ方針・認証認可・個人情報セクションを具体化
- `.claude/knowledge/security/security-principles.md`: 脅威モデル・保護すべき資産を vibehawk 固有の内容で記入

### 最終判断

- 承認: セキュリティリスクなし（MVV・設計ポリシーの内容自体は適切）
- 追記のみ: 知見を SECURITY.md と security-principles.md に反映した

---

## 2026-05-16 — Issue #140 / PR dev/140_rewrite_status_check_positioning

### 対象

docs 3 ファイル（POLICY.md / SECURITY.md / specification.md）の status check 主軸 positioning 書き換え。コード変更ゼロ。

### 合議状況

- セキュリティチーム結果: 全員一致（問題なし）
- 全会一致ルール: 適用なし（3件中 Major 以上の指摘ゼロ）

### メタレビュー判定

- 脆弱性: なし
- 攻撃チェーン: リスクなし（コード変更ゼロ、攻撃面の変化なし）
- SECURITY.md 整合: 問題なし（経路 ①' の `checks: write` 記述と整合）
- 見落とし: あり（本 PR スコープ外）: SECURITY.md L.59 の `id-token: write` 禁止列挙が `autonomous-restrictions.md` §6 例外承認（Issue #505）と未整合。本 PR 以前から存在する既存矛盾。別 Issue で補正推奨
- 過去判断との一貫性: 一貫（2026-Q2 ship #6 判断と整合）

### 脅威評価

- 深刻度: なし
- 攻撃チェーン: 該当なし（docs only 変更）
- 影響範囲: なし

### 最終判断

- 承認: セキュリティリスクなし。CISOブロッキング指摘なし
- 付記: SECURITY.md L.59 の `id-token: write` 記述不整合は別 Issue での補正を推奨（「原則禁止、ただし npm provenance publish 目的および Issue #505 CISO 承認済の例外を除く」への更新）

---

## 2026-05-24 — Issue #219 sticky walkthrough コメント実装

### 対象

`vibehawk-review.yml` / `vibehawk-review-skip-mark.yml` への sticky upsert step 追加、`skip-mark.yml` の `issues: write` 追加、新規スクリプト `post-sticky-comment.sh`。

### 合議状況

- セキュリティチーム結果: 全員一致（問題なし）
- 全会一致ルール: 適用なし（脆弱性検出なし）

### メタレビュー判定

- 脆弱性: なし
- 攻撃チェーン: リスクなし（bot 名義 + マーカー filter による DELETE 事前検証、プロンプトインジェクション干渉なし）
- SECURITY.md 整合: 問題なし（`issues: write` は L.193-195 の既承認スコープ内）
- secrets-handling.md 整合: 問題なし（新規 secrets なし・App Installation Token 流用は同一 run 内・1h 失効）
- autonomous-restrictions.md §6: 抵触なし（編集対象は `claude*.yml` / `ai-review.yml` に非該当、追加権限は禁止権限外）
- 見落とし: なし
- 過去判断との一貫性: 一貫（2026-Q2 ship #6 の App 最小権限判断と整合）

### 脅威評価

- 深刻度: なし
- 攻撃チェーン: 該当なし
- 影響範囲: なし

### 最終判断

- 承認: セキュリティリスクなし

---

## 2026-06-03 — Epic #305 子 Issue A/B/C 自律実行不可領域チェック

### 対象

Epic #305 の子 Issue 3 件が `.claude/rules/autonomous-restrictions.md` の 6 分類不可領域に該当するかの判定。

- 子A: バージョン SoT の `package.json` 一本化（`plugin.json` 食い違い解消）
- 子B: main マージで semantic-release が自動 Release を作成する workflow 追加
- 子C: version 未 bump の PR に警告コメントを投稿する workflow 追加

### 合議状況

- セキュリティチーム結果: 本件は合議形式でなく、CISO 独自判断として実施（不可領域フィルタリング）
- 全会一致ルール: 適用なし（脆弱性審査ではなく領域判定）

### メタレビュー判定

- 脆弱性: なし（各 Issue はバージョン管理・CI 整備の範囲内）
- 攻撃チェーン: リスクなし（禁止権限追加なし、Fork PR 除外条件は遵守方向、secrets 参照変更なし）
- SECURITY.md 整合: 問題なし（子B の `id-token: write` は npm provenance 用として既承認済み）
- 見落とし: なし
- 過去判断との一貫性: 一貫（2026-05-24 の `issues: write` 追加判断と同様の最小権限原則を適用）

### 脅威評価

- 深刻度: なし
- 攻撃チェーン: 該当なし
- 影響範囲: なし

### 最終判断

- 子A: OK — 6 分類のいずれにも抵触しない
- 子B: OK — `release.yml` 拡張だが禁止権限追加なし。既存 `id-token: write` は npm provenance 用として既承認（SECURITY.md §「CISO Critical 条件 3 点」）
- 子C: OK — 最小権限（`contents: read` / `pull-requests: write`）、Fork PR 除外条件を遵守する方向の実装、secrets 参照なし

---

## 2026-06-12 — Issue #341 .claude/.gitignore skills 例外化 + release SKILL.md 新規

### 対象

`.claude/.gitignore` の `skills/*` 一括 ignore + `!skills/release/` 例外化、`.claude/skills/release/SKILL.md` 新規追加（版上げ係スキル、12 ステップ）、`docs/specification.md` 追記（3 行）、`tests/test_release_skill.sh` 新規追加。

### 合議状況

- セキュリティチーム結果: 分裂（analyst 1「問題なし」、analyst 2「要注意 M-1/M-2」、analyst 3「要注意 M-1」）
- 全会一致ルール: 適用なし（両 Major が確定事実で棄却されたため）

### メタレビュー判定

- 脆弱性: なし
- 攻撃チェーン: リスクなし（diagnose-guard.sh はパスパターン deny、gitignore 例外化はガードに無影響。npm publish 連鎖は既存 GHA OIDC 経路で完結）
- SECURITY.md 整合: 問題なし（認証・認可・権限スコープ変更なし）
- 見落とし: なし（analyst 2/3 両 Major は確定事実 COO 実機確認で棄却）
- 過去判断との一貫性: 一貫（2026-05-24 Issue #219 と同型: CEO 明示実行 + 3 者ゲート通過済み）

#### Major 棄却根拠

- analyst 2 M-1（§4 ガードレール領域該当）: 自律実行禁止であり人間承認ルートは autonomous-restrictions.md が明示許容。CEO 明示 /ship 実行・起票時 3 者ゲート通過・guide-gate スタンプ CEO 承認済み。前提誤認により棄却。
- analyst 3 M-1（diagnose-guard が git 追跡依存か）: COO 実機確認により diagnose-guard.sh がパスパターン deny（`skills/.*$` 正規表現、realpath 正規化込み）と確認。git 追跡状態に一切依存しない。前提崩壊により棄却。

#### 残存採用指摘

- **採用（推奨・非ブロッカー）**: `tests/test_release_skill.sh` の `git check-ignore -q` が exit 128 を返す場合（リポジトリ外実行）に `else` 節で誤 pass となる。CI 環境では発生しないが、テスト整合（intent/infra 重視軸）として exit 128 ガードの追加を推奨。

### 脅威評価

- 深刻度: なし
- 攻撃チェーン: 該当なし
- 影響範囲: なし

### 最終判断

- 承認: セキュリティリスクなし
- 付記: `test_release_skill.sh` の `git check-ignore` exit 128 ガード追加を推奨（Minor・非ブロッカー）

---

## 2026-06-12 — Issue #344 README.md 再構成（30 秒スキャン構造化）

### 対象

`README.md`（263 行）を vibecorp 流 30 秒スキャン構造に再編する計画（`intent/docs`、挙動不変）のメタレビュー。実装計画ファイル: `~/.cache/vibecorp/plans/vibehawk-64a995f1/dev/344_readme_restructure.md`。

### 合議状況

- セキュリティチーム結果: 全員一致（要注意・確認推奨）— 3 名全員が「文言保全のテスト基準欠如」系の Medium 指摘を検出
- 全会一致ルール: 適用（3 名中 1 名以上が Medium 指摘を検出）

### メタレビュー判定

- 脆弱性: なし（直接的な脆弱性・コード変更なし）
- 攻撃チェーン: 要注意（低強度）: 「README のセキュリティ記述の言い換え → 利用者の誤った導入 → CISO Critical 条件の形骸化」という間接チェーン（2-3 ホップ、実現確率は低い）
- SECURITY.md 整合: 要確認（CISO Critical 条件 5 フレーズが計画の検証対象リストに未明示）
- 見落とし: あり（文言保全 grep テストが計画に不在）
- 過去判断との一貫性: 一貫（ただし本件は 263 行全体の再構成であり Issue #140 等の部分書き換えとは規模が異なる）

### 保全対象フレーズ（5 件、CISO 確認）

SECURITY.md / README の以下 5 記述が再構成後も保全されることを機械的（grep）に確認する必要がある。

1. OAuth Token stdout 非出力: 「stdout に出さない」相当（SECURITY.md L.336 の CISO Critical 条件）
2. 3 secrets 手動登録: 「3 secrets」「手動登録」の両語（SECURITY.md L.119）
3. 経路 2 のみ・経路 1 を認めない否定的保証: 「経路 1 を標準経路として認めない」または「経路 1 は OSS 利用者の標準導入経路として認めない」（SECURITY.md L.168）
4. Anthropic 送信開示: 「Anthropic に送信する」相当（README L.134-136）
5. ANTHROPIC_API_KEY 中止挙動: 「実行を中止」相当（README L.208-211 の [!NOTE] コールアウト）

### 脅威評価

- 深刻度: 低
- 攻撃チェーン: 実装コードへの直接攻撃面なし。セキュリティ設計の保証文言がドキュメント再構成で消えることによる利用者への情報開示品質の低下が間接リスク
- 影響範囲: README を読んで導入する新規利用者の情報開示品質

### 最終判断

- 条件付き承認: 以下 3 点を計画ファイルに追記した上で実装に移行してよい
  1. タスク 10「セキュリティ文言保全検証」（grep ベース、上記 5 フレーズ）を追加する
  2. テスト計画に「CISO Critical 条件の 5 フレーズが grep で保全されている（タスク 10）」を追加する
  3. タスク 5 のテスト基準に「ANTHROPIC_API_KEY 中止挙動の [!NOTE] コールアウトが形式（コールアウト種別 NOTE）ごと維持されていること」を追記する

---

## 2026-06-13 — Issue #346 / PR #348 配布 workflow の自己完結化（runtime checkout 導入）

### 対象

配布 workflow を自己完結化するため、pin 付き 2nd checkout（runtime checkout）で vibehawk 本体を実行時に取得する構成（`intent/infra`）のメタレビュー。

### 合議状況

- セキュリティチーム結果: 全員一致（mutable タグ pin を Major と判定: 3/3 一致）
- 全会一致ルール: 適用（3 名全員が Major 指摘を検出）

### メタレビュー判定

- 脆弱性: 検出: mutable タグ pin（後述）。緩和策適用＋恒久対応起票で条件付き承認
- 攻撃チェーン: 要注意: タグ差し替え → runtime checkout で悪性コード取得 → LLM 実行文脈へ到達（緩和済み）
- SECURITY.md 整合: 問題なし（信頼境界・残余リスクを PR #348 内で SECURITY.md に記録済み）
- 見落とし: なし
- 過去判断との一貫性: 一貫（ship #6 の Installation Token 設計・#305 の `id-token` 既承認スコープと矛盾なし）

### 判断詳細（runtime checkout の条件付き承認: 3 条件）

1. **`persist-credentials: false` 必須**: LLM が workspace を読む構成のため、checkout token を `.git/config` に残さない。残すと「LLM がリポジトリ内容を読む → token 混入文字列を出力に反映」の漏洩経路が開く。
2. **mutable タグ pin は Major（security-analyst 3/3 一致）**:
   - 緩和（即時）: `v*` タグの update / delete を禁止する ruleset を適用。creation は `release-tag.yml` のリリース動線のため許可、bypass actors は空。
   - 恒久対応: SHA pin 化（Issue #347 起票済み）。
3. **hashFiles guard の評価位置（PR head 支配下）は増分リスクゼロ判定**: `pull_request` トリガーでは workflow 定義自体が PR 由来であり、guard を消す攻撃者は workflow ごと書き換えられる（guard は防御境界ではない）。fork PR には secrets が供給されないため攻撃が成立しない。→ コード変更不要。SECURITY.md に残余リスクとして記録して止血。

### 付随判断・運用知見

- **include 基点の変更（git toplevel → スクリプト位置基準）**: 対象リポジトリ側のファイルが include されるプロンプト注入面を閉じる改善として承認。
- **ruleset の自動設定不可**: ruleset 適用を Claude Code から実行しようとしたところ permission classifier に拒否された（ガバナンス変更はユーザー未依頼のため）→ CEO 操作に切替。**運用知見: リポジトリ設定変更（ruleset / branch protection 等）はエージェント自律実行の対象外とし、CEO 操作に委ねる。**

### 脅威評価

- 深刻度: 中（緩和前: mutable タグ差し替えによるサプライチェーン攻撃）→ 緩和後: 低
- 攻撃チェーン: タグ update 権限者（または権限奪取者）がタグを悪性 commit へ差し替え → 配布先の runtime checkout が悪性コードを取得。ruleset（update/delete 禁止・bypass 空）で経路を遮断、SHA pin（#347）で恒久封鎖予定
- 影響範囲: vibehawk を配布 workflow 経由で導入する全リポジトリ

### 最終判断

- 条件付き承認: ① `persist-credentials: false`（適用済み）② `v*` タグ保護 ruleset（CEO 操作で適用）＋ SHA pin 化を Issue #347 で恒久対応 ③ hashFiles guard はコード変更不要・SECURITY.md 残余リスク記録（PR #348 内で記録済み）
